'use strict';

var fs = require('fs'),
    http = require('http'),
    assert = require('assert');

describe('Sabre Sessions', function() {

	it('should show any message', function(done) {
		var err = "prueba";
		//assert.ok(err);
		assert.notStrictEqual(err, null)
	    done();
	});
});    